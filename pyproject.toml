[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "five-guys"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "asyncio>=4.0.0",
    "pygame>=2.6.1",
]

[project.scripts]
five-guys = "main:main"

